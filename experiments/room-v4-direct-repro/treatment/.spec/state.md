# Treatment State

- Phase: K1 bootstrap → fixed Brief implementation.
- Scope: this `treatment/` directory only; native HTML/CSS/JavaScript and localStorage.
- Gate audit: kernel created before application files; all current observable results map one-to-one to R1–R12 contracts in K1.
- Current task: fresh correction and Chromium verification complete; R1–R12
  pass after removing the form-reset runtime exception.
- Out of scope: control/root edits, dependencies, server, schemas, generators, runtime browser claims before testing.
- Evidence: `node --check app.js`, file order/cost, correction details, and the
  complete R1–R12 Chromium result are recorded.
