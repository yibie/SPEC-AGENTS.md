# Treatment arm

Before touching the copied app, record `revise` and the D5 compatible
alternative in `.spec/kernel.md`, `.spec/state.md`, and `.spec/evidence.md`.
Then implement only D5 and record R1–R13 evidence.
